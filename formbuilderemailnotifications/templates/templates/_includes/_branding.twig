<section class="template-cmp-tout cmp-tout section-collapsible">
    <header class="theme-{{ theme is defined ? theme }}">
        <div class="titlebar">
            <span class="tout-title">{{ "Branding" |t }}</span>
        </div>
        <div class="actions">
            <a class="icons collapser bodytoggle" title="Collapse" role="combobox" tabindex="0"></a>
        </div>
    </header>

    <div class="body">
        {% include 'emailnotifications/templates/_includes/branding/_logo' ignore missing %}
        {% include 'emailnotifications/templates/_includes/branding/_logo-position' ignore missing %}
    </div>
</section>